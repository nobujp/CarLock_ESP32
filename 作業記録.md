# CarLock ESP32 作業記録

## ハードウェア構成（確認済み）

- **ボード**: Aideepen ESP32 4chリレーモジュール
- **通信**: BLE (Bluetooth Low Energy) Nordic UART Service
- **操作アプリ**: Serial Bluetooth Terminal (Android) / BlueSee (iOS)
- **シリアル速度**: 115200 bps / COM8

### リレーピン配置（スキャンテストで確認済み）

| リレー | GPIO | 役割 |
|--------|------|------|
| RELAY_1 | 23 | ロック |
| RELAY_2 | 5 | アンロック |
| RELAY_3 | 4 | ハザード / アンサーバック |
| RELAY_4 | 13 | 予備 |

- **動作極性**: active LOW（LOW=ON, HIGH=OFF）

---

## コマンド一覧

| 送信コマンド | 動作 |
|------------|------|
| `1234-L` | ロック（RELAY_1 500ms + ハザード1回） |
| `1234-U` | アンロック（RELAY_2 500ms + ハザード2回） |
| `1234-H` | ハザード5秒間点滅 |
| `1234-R4` | RELAY_4 500ms（予備） |
| `1234-STATUS` | 状態確認（IDLE/BUSY） |

---

## 動作確認済み項目

- [x] 起動時に全リレーがOFF
- [x] BLE接続・切断の検出
- [x] PINコード認証
- [x] ロック（1234-L）: RELAY_1 動作 + ハザード同時1回点滅
- [x] アンロック（1234-U）: RELAY_2 動作 + ハザード同時2回点滅
- [x] ハザード（1234-H）: 5秒間点滅後に自動停止
- [x] 間違いコマンドのエラー返信

---

## トラブルと解決策

### 1. 起動時に全リレーがON
- **原因**: `RELAY_ON=HIGH, RELAY_OFF=LOW` の極性設定が逆だった
- **解決**: `RELAY_ON=LOW, RELAY_OFF=HIGH` に変更（active LOW確認）

### 2. 起動時にRELAY_3だけONのまま
- **原因**: `pinMode(OUTPUT)` にする瞬間ピンがLOWになりリレーがONになった
- **解決**: `digitalWrite(HIGH)` → `pinMode(OUTPUT)` の順番に変更

### 3. BLEコマンドを受信しているのにリレーが動かない
- **原因**: BLEは別タスク（Core0）で動作するため、コールバック内の `digitalWrite()` が物理的に反映されなかった
- **解決**: コールバック内では `pendingCmd` フラグを立てるだけにして、`loop()` 内でGPIO操作を実行

### 4. ロック/アンサーバックのタイミングがずれる
- **原因**: ロック完了後にハザードを開始していたため遅延が発生
- **解決**: ロック/アンロックリレーと同時にハザードも開始

---

## セキュリティ強化（2026-06-18 実装済み）

| 機能 | 内容 |
|------|------|
| PIN ロックアウト | 3回失敗 → 60秒間操作不可 |
| NVS 保存 | フラッシュにPINを記録、電源OFFでも消えない |
| PIN変更コマンド | `現PIN-SETPIN:新PIN` でBLE経由変更 |
| スマホアプリ対応 | 設定画面「デバイスのPINを変更」ボタン追加 |

### バッテリー上がりとPINについて
- バッテリーが上がってもPINは **消えない**（フラッシュは不揮発性メモリ）
- PINが `1234` に戻るのは PlatformIO で **Erase Flash した場合のみ**

### PINを変更する手順
1. スマホアプリを開いて接続
2. ⚙️ 設定を開く
3. 「デバイスのPINを変更」欄に新PINを入力（4〜8桁）
4. 「変更する」をタップ
5. ログに「✅ PINを ○○○○ に変更・保存しました」と出れば完了

---

## スマホアプリ（GitHub Pages）

- URL: `https://nobujp.github.io/CarLock_ESP32/`
- 初回：接続ボタン → CarLock-ESP32 を選択
- 2回目以降：自動接続
- iOS：Bluefy ブラウザ必須 / Android：Chrome

---

## 今後の改造候補

- [ ] BLE切断時に自動ロック
- [ ] 未接続5分でタイムアウト自動ロック
- [ ] RELAY_4をエンジンスタート（3秒パルス）に変更
- [ ] 複数PINコード登録（サブPIN追加）

---

## ファイル構成

```
CarLock_ESP32_PIO/
├── src/
│   └── main.cpp       ← メインプログラム
├── platformio.ini     ← PlatformIO設定
├── 作業記録.md        ← この記録ファイル
└── .gitignore
```

---

---

## 現在のプログラム (src/main.cpp)

```cpp
/**
 * ESP32 4-Relay Car Lock Controller
 * Board : Aideepen ESP32 Relay Module (4ch)
 * Control: BLE (Nordic UART Service)
 * App   : "Serial Bluetooth Terminal" (Android) / "BlueSee" (iOS)
 *
 * コマンド形式: "PINコード-コマンド"
 *   例) 1234-L  → ロック
 *       1234-U  → アンロック
 *       1234-H  → 5秒ハザード
 *       1234-R4 → RELAY_4 (予備)
 *       1234-STATUS → 状態確認
 */

#include <Arduino.h>
#include <BLEDevice.h>
#include <BLEServer.h>
#include <BLEUtils.h>
#include <BLE2902.h>

// ==================== 設定 ====================

#define BLE_DEVICE_NAME  "CarLock-ESP32"

const String PIN_CODE = "1234";

#define RELAY_1  23   // ロック
#define RELAY_2   5   // アンロック
#define RELAY_3   4   // ハザード / アンサーバック
#define RELAY_4  13   // 予備

#define RELAY_ON   LOW    // active LOW確認済み
#define RELAY_OFF  HIGH

#define LOCK_PULSE_MS     500   // ロック/アンロック ON時間
#define HAZARD_5SEC_MS   5000   // ハザード継続時間
#define HAZARD_BLINK_MS   500   // ハザード点滅間隔

// ===============================================

#define SERVICE_UUID            "6E400001-B5A3-F393-E0A9-E50E24DCCA9E"
#define CHARACTERISTIC_UUID_RX  "6E400002-B5A3-F393-E0A9-E50E24DCCA9E"
#define CHARACTERISTIC_UUID_TX  "6E400003-B5A3-F393-E0A9-E50E24DCCA9E"

BLEServer*         pServer           = nullptr;
BLECharacteristic* pTxCharacteristic = nullptr;
bool               deviceConnected   = false;

// BLEコールバック→loop()へのコマンド受け渡し用フラグ
volatile int pendingCmd = 0;

unsigned long relayOffTime = 0;
int           activeRelay  = -1;

bool          isBlinking      = false;
bool          blinkState      = false;
unsigned long blinkEndTime    = 0;
unsigned long blinkNextToggle = 0;

// ロック/アンロック後のハザードフィードバック
int           feedbackBlinks     = 0;
bool          feedbackActive     = false;
bool          feedbackState      = false;
unsigned long feedbackNextToggle = 0;

// ==================== BLE送信 ====================
void sendBLE(const String& msg) {
    Serial.println("TX: " + msg);
    if (deviceConnected && pTxCharacteristic) {
        pTxCharacteristic->setValue(msg.c_str());
        pTxCharacteristic->notify();
        delay(10);
    }
}

// ==================== リレー制御 (loop()内でのみ呼ぶ) ====================
void activateRelay(int pin, unsigned long durationMs, const String& ackMsg) {
    if (activeRelay != -1 || isBlinking || feedbackActive) {
        sendBLE("BUSY");
        return;
    }
    digitalWrite(pin, RELAY_ON);
    activeRelay  = pin;
    relayOffTime = millis() + durationMs;
    sendBLE(ackMsg);

    // ロック/アンロックと同時にハザードフィードバック開始
    if (pin == RELAY_1 || pin == RELAY_2) {
        feedbackBlinks     = (pin == RELAY_1) ? 1 : 2;
        feedbackActive     = true;
        feedbackState      = true;
        digitalWrite(RELAY_3, RELAY_ON);
        feedbackNextToggle = millis() + 400;
    }
}

void startHazard() {
    if (activeRelay != -1 || isBlinking || feedbackActive) {
        sendBLE("BUSY");
        return;
    }
    isBlinking      = true;
    blinkState      = true;
    blinkEndTime    = millis() + HAZARD_5SEC_MS;
    blinkNextToggle = millis() + HAZARD_BLINK_MS;
    digitalWrite(RELAY_3, RELAY_ON);
    sendBLE("HAZARD_ON");
}

// ==================== BLEコールバック ====================
class ServerCallbacks : public BLEServerCallbacks {
    void onConnect(BLEServer* pSrv) override {
        deviceConnected = true;
        Serial.println("BLE: 接続");
    }
    void onDisconnect(BLEServer* pSrv) override {
        deviceConnected = false;
        Serial.println("BLE: 切断 → 再アドバタイズ");
        pSrv->startAdvertising();
    }
};

class RxCallbacks : public BLECharacteristicCallbacks {
    void onWrite(BLECharacteristic* pChar) override {
        String rx = String(pChar->getValue().c_str());
        rx.trim();
        Serial.println("RX: " + rx);

        int sep = rx.indexOf('-');
        if (sep == -1 || rx.substring(0, sep) != PIN_CODE) {
            sendBLE("ERR:AUTH_FAIL");
            return;
        }

        String cmd = rx.substring(sep + 1);
        cmd.toUpperCase();

        if      (cmd == "L" || cmd == "LOCK")   pendingCmd = 1;
        else if (cmd == "U" || cmd == "UNLOCK")  pendingCmd = 2;
        else if (cmd == "H" || cmd == "HAZARD")  pendingCmd = 3;
        else if (cmd == "R4")                    pendingCmd = 4;
        else if (cmd == "STATUS")                pendingCmd = 9;
        else                                     sendBLE("ERR:UNKNOWN_CMD [" + cmd + "]");
    }
};

// ==================== setup ====================
void setup() {
    Serial.begin(115200);
    Serial.println("\n=== CarLock ESP32 起動 ===");

    const int relayPins[] = { RELAY_1, RELAY_2, RELAY_3, RELAY_4 };
    for (int pin : relayPins) {
        digitalWrite(pin, RELAY_OFF);
        pinMode(pin, OUTPUT);
        digitalWrite(pin, RELAY_OFF);
    }
    Serial.println("リレー: 全OFF");

    BLEDevice::init(BLE_DEVICE_NAME);
    pServer = BLEDevice::createServer();
    pServer->setCallbacks(new ServerCallbacks());

    BLEService* pService = pServer->createService(SERVICE_UUID);

    pTxCharacteristic = pService->createCharacteristic(
        CHARACTERISTIC_UUID_TX, BLECharacteristic::PROPERTY_NOTIFY);
    pTxCharacteristic->addDescriptor(new BLE2902());

    BLECharacteristic* pRxCharacteristic = pService->createCharacteristic(
        CHARACTERISTIC_UUID_RX,
        BLECharacteristic::PROPERTY_WRITE | BLECharacteristic::PROPERTY_WRITE_NR);
    pRxCharacteristic->setCallbacks(new RxCallbacks());

    pService->start();
    pServer->getAdvertising()->start();

    Serial.println("BLE アドバタイズ開始: " BLE_DEVICE_NAME);
    Serial.println("PIN: " + PIN_CODE);
    Serial.println("準備完了 - スマホから接続してください");
}

// ==================== loop ====================
void loop() {
    unsigned long now = millis();

    if (pendingCmd != 0) {
        int cmd = pendingCmd;
        pendingCmd = 0;
        switch (cmd) {
            case 1: activateRelay(RELAY_1, LOCK_PULSE_MS, "LOCKING");   break;
            case 2: activateRelay(RELAY_2, LOCK_PULSE_MS, "UNLOCKING"); break;
            case 3: startHazard();                                       break;
            case 4: activateRelay(RELAY_4, LOCK_PULSE_MS, "RELAY4_ON"); break;
            case 9: sendBLE(activeRelay == -1 ? "STATUS:IDLE" : "STATUS:BUSY"); break;
        }
    }

    // ロック/アンロック 自動OFF
    if (activeRelay != -1 && now >= relayOffTime) {
        digitalWrite(activeRelay, RELAY_OFF);
        sendBLE("RELAY_OFF:GPIO" + String(activeRelay));
        activeRelay = -1;
    }

    // ハザードフィードバック点滅 (ロック後1回 / アンロック後2回)
    if (feedbackActive) {
        if (now >= feedbackNextToggle) {
            if (!feedbackState) {
                digitalWrite(RELAY_3, RELAY_ON);
                feedbackState      = true;
                feedbackNextToggle = now + 400;
            } else {
                digitalWrite(RELAY_3, RELAY_OFF);
                feedbackState = false;
                feedbackBlinks--;
                if (feedbackBlinks <= 0) {
                    feedbackActive = false;
                } else {
                    feedbackNextToggle = now + 400;
                }
            }
        }
    }

    // ハザード点滅 (1234-H コマンド)
    if (isBlinking) {
        if (now >= blinkEndTime) {
            digitalWrite(RELAY_3, RELAY_OFF);
            isBlinking = false;
            sendBLE("HAZARD_OFF");
        } else if (now >= blinkNextToggle) {
            blinkState = !blinkState;
            digitalWrite(RELAY_3, blinkState ? RELAY_ON : RELAY_OFF);
            blinkNextToggle = now + HAZARD_BLINK_MS;
        }
    }
}
```

---

---

## 自宅PCでの環境構築手順

### 必要なもの
- Windows PC
- USBケーブル（ESP32接続用）
- インターネット接続

### 手順

**① VSCode インストール**
```
https://code.visualstudio.com/
```
ダウンロードしてインストールする

**② PlatformIO拡張機能をインストール**
1. VSCodeを開く
2. 左側の拡張機能アイコン（四角いマーク）をクリック
3. 検索欄に「PlatformIO」と入力
4. 「PlatformIO IDE」→「インストール」

**③ GitHubからプロジェクトをダウンロード**
1. VSCodeのターミナルを開く（Ctrl + @）
2. 以下を入力してEnter：
```
git clone https://github.com/nobujp/CarLock_ESP32.git
```

**④ フォルダを開く**
1. VSCode → ファイル → フォルダを開く
2. ダウンロードした `CarLock_ESP32` フォルダを選択

**⑤ ESP32に書き込む**
1. ESP32をUSBで接続
2. PlatformIO → Upload をクリック
3. `[SUCCESS]` が出れば完了

### GitHubリポジトリ
```
https://github.com/nobujp/CarLock_ESP32
```

### スマホアプリURL
```
https://nobujp.github.io/CarLock_ESP32/
```

---

*最終更新: 2026-06-18*
